{
  "name": "credvista-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix frontend && pip install -r backend/requirements.txt",
    "frontend": "npm run dev --prefix frontend",
    "backend": "cd backend && python app.py",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"cd backend && python app.py\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
